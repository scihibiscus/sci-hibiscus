import React from 'react'
import styles from "../../styles/ren"

interface HeroProps {
  text: string;
  heading: string;
}

const RentBuyHero = ({
  content,
}: {
  content: HeroProps;
}) => {
  return (
    <div className={}>

    </div>
  )
}

export default RentBuyHero