import React from 'react'
import styles from "../../styles/"

interface HeroProps {
  text: string;
  heading: string;
}

const RentBuyHero = ({
  content,
}: {
  content: HeroProps;
}) => {
  return (
    <div className={}>

    </div>
  )
}

export default RentBuyHero