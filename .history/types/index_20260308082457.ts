export interface TransactionData {
  nameen: string,
  namefr: string,
  slug: {
    _type: string,
    current: string
  }
}

export interface TData {
  nameen: string,
  namefr: string,
  slug: {
    _type: string,
    current: string
  }
}