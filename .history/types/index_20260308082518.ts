export interface TransactionData {
  nameen: string,
  namefr: string,
  slug: {
    _type: string,
    current: string
  }
}

export interface LocationsData {
  cityname: string,
  slug: {
    _type: string,
    current: string
  }
}