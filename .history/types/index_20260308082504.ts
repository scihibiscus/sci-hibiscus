export interface TransactionData {
  nameen: string,
  namefr: string,
  slug: {
    _type: string,
    current: string
  }
}

export interface LocationsData {
  nameen: string,
  namefr: string,
  slug: {
    _type: string,
    current: string
  }
}